apiVersion: v1
kind: Service
metadata:
  name: {{ include "rate-limiter-chart.fullname" . }}
  labels:
    {{- include "rate-limiter-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rate-limiter-chart.selectorLabels" . | nindent 4 }}
