apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rate-limiter-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rate-limiter-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rate-limiter-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
